apiVersion: v1
kind: ConfigMap

metadata:
  name: thumbnail-service-config
  namespace: "{{ .Release.Namespace }}"

data:
  MINIO_ENDPOINT: "{{ .Values.config.minioEndpoint }}"
  MINIO_BUCKET: "{{ .Values.config.minioBucket }}"
  MINIO_SECURE: "{{ .Values.config.minioSecure }}"
